// RecentRatingsTable is no longer used — the live ratings table is now
// embedded directly in CSATSatisfaction.tsx.
// This file is kept as an empty shim to avoid breaking any stale imports.
export function RecentRatingsTable() { return null; }
