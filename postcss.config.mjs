/**
 * PostCSS Configuration
 *
 * Tailwind CSS v4 runs through the `@tailwindcss/postcss` plugin under
 * Next.js (the Vite plugin used previously does not apply here).
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
